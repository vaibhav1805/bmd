---
project: BMD Terminal Markdown Viewer
task: Theme Switching Implementation
status: paused
last_updated: 2026-02-28T12:00:00Z
---

## Current State

Working on adding runtime theme switching to BMD. User wants to press a key (e.g., 'T') while viewing to open a theme selector overlay.

**Completed Work:**
- ✅ Added 4 theme color presets (Ocean, Forest, Sunset, Midnight) in `internal/theme/colors.go`
- ✅ Added mouse scroll support
- ✅ Added screen margins (top + left padding)
- ✅ Reduced block spacing for elegant layout
- ✅ Added web link opening in browser
- ✅ Enhanced UI with colors and emoji

**In Progress:**
- Theme switching UI - NOT STARTED
  - Need to add theme selector state to Viewer struct
  - Need key binding (e.g., 't' key) to open theme list
  - Need overlay UI to display available themes
  - Need logic to re-render document with new theme

## Remaining Work

1. **Add theme selector state to Viewer struct**
   - `themeOpen bool` - whether theme selector is visible
   - `themeSel int` - currently selected theme index
   - List of available themes: Ocean, Forest, Sunset, Midnight, Default (Indigo)

2. **Add key binding in Update()**
   - Press 't' to toggle theme selector open/closed
   - Up/Down arrow to navigate themes
   - Enter to select theme
   - Esc to cancel

3. **Create theme selector overlay**
   - Similar to help overlay rendering
   - Display list of theme names with colors
   - Highlight currently selected theme

4. **Implement theme switching logic**
   - Re-render entire document with new theme
   - Update `v.Theme` field
   - Call renderer with new theme
   - Update `v.Lines` and `v.rawLines`
   - Show "Theme changed to X" in status bar

5. **Test and polish**
   - Verify all themes render correctly
   - Ensure smooth theme switching
   - No visual glitches

## Key Files to Modify

- `internal/tui/viewer.go` - Add theme selector state, key binding, updateTheme()
- `internal/tui/render.go` - Add renderThemeSelector() function
- Possibly add theme persistence to config file (optional enhancement)

## Architecture Notes

Similar to how help overlay works:
- Add `themeOpen` state to Viewer
- Route 't' key to new `updateThemeSelector()` function
- Render theme list overlay when `themeOpen == true`
- On selection, re-render entire document and close overlay

## Next Action

When resuming:
1. Add `themeOpen bool` and `themeSel int` to Viewer struct
2. Add 't' key binding case in Update() -> calls updateThemeSelector()
3. Create updateThemeSelector() and renderThemeSelector() functions
4. Test with key binding

## Notes

- Themes are already defined in `internal/theme/colors.go`
- Don't need config file initially (can add later)
- Keep theme selection simple and fast
- Similar UX to help overlay ('?' key)
