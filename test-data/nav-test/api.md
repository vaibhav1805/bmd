# API Reference

Back to [Index](./index.md).

## Functions

- `ResolveLink(currentFile, href, startDir)` — resolves a relative link safely
- `FindMatches(lines, query)` — finds case-insensitive occurrences

The navigation history stack supports push, back, and forward operations.
