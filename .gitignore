# Planning & Claude files
.planning/
.claude/
.continue-here.md
.handoff/
AGENTS.md
CLAUDE.md

# IDE & Editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Build artifacts
/bmd
bin/bmd*
vendor/
*.test
*.out

# Build artifacts (named binaries — not covered by /bmd root-only pattern)
bmd-final
bmd-test

# Index and graph export files
*.bmd-tree.json
*.bmd-index.json
.bmd-registry.json
.bmd-llm-extractions.json
.bmd-relationships-discovered.yaml
.bmd-relationships.yaml

# BMD artifacts directory (PageIndex trees, temp files)
.bmd/

# Dependencies
node_modules/

# Logs
*.log

# Database
*.db
knowledge.db
.planning/

# Beads / Dolt files (added by bd init) — issue data lives in the Dolt remote,
# not git; run `bd init` on a fresh clone to regenerate local config/hooks.
.dolt/
.beads-credential-key
.beads/
